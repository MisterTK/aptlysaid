# Local Development Environment Variables
# Copy this to .env.local and fill in your values

# Supabase Local Development (from supabase start)
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-local-anon-key-from-supabase-start
SUPABASE_SERVICE_ROLE_KEY=your-local-service-role-key

# For production and preview deployments:
# These are automatically set by Vercel + Supabase Integration
# No need to configure them manually!

# Optional: Local testing of external services
STRIPE_SECRET_KEY=sk_test_xxx  # Use Stripe test key for local dev
GOOGLE_OAUTH_CLIENT_ID=xxx     # Optional for local OAuth testing
GOOGLE_OAUTH_CLIENT_SECRET=xxx # Optional for local OAuth testing